<div>
    <img src="{{beer.image_url}}" alt="beer" />
    <h2>{{beer.name}}</h2>
    <h3>{{beer.tagline}}</h3>
    <p>{{beer.description}}</p>

    <ul>
     {{#each beer.food_pairing}}
     <li>{{this}}</li>
     {{/each}}
  </ul>
    </ul>
</div>